Legacy order-book scripts

These scripts exist for historical debugging/migration of the retired player order-book system.
The app is AMM-only for player trading.

Notes:
- This folder is intentionally not part of the production server runtime.
- These scripts may reference the legacy `orders` table and/or endpoints that no longer exist.
